# Tutor Agent MySQL 资源参数（数据库重构文档 §3.B1，2C4G 同机部署）
[mysqld]
innodb_buffer_pool_size = 256M
max_connections         = 50
performance_schema      = OFF   # 省约 200MB；项目有自建 trace 表
# sql_mode 不覆盖：MySQL 8.4 默认已含 STRICT_TRANS_TABLES（验收时确认）
