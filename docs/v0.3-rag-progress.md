# v0.3 RAG 实现进度

这份文档记录 v0.3 RAG「能用」阶段的实际完成度、实现细节和验证结果。主设计见 `docs/v0.3 RAG设计文档.md`。

## 2026-07-07：完成 3.1 / 3.2

### 已完成

- [x] `app/services/knowledge_chunker.py`：新增 Markdown 分块器。
- [x] `app/services/rag_settings.py`：新增 RAG 基础参数 `CHUNK_SIZE`、`CHUNK_OVERLAP`、`EMBEDDING_BATCH_SIZE`。
- [x] `app/clients/embedding_client.py`：新增 OpenAI-compatible embedding 客户端。
- [x] `app/config.py`：新增 `EmbeddingConfig` 和 `load_embedding_config()`。
- [x] `.env.example`：新增 `EMBEDDING_MODEL` 示例配置。
- [x] `tests/test_knowledge_chunker.py`：覆盖分块器核心行为。
- [x] `tests/test_embedding_client.py`：覆盖 embedding 配置和客户端行为。

### 实现细节

- 分块器是纯函数，不读写文件，便于单测和后续索引脚本复用。
- 标题只识别 `#` 到 `###`，并维护标题栈生成 `title_path`，例如 `Agent 架构设计 > 非目标 > 测试策略`。
- 空章节不会进入索引，避免只有标题、没有正文的内容污染检索结果。
- 超长内容使用固定滑窗切分，默认 `CHUNK_SIZE=512`、`CHUNK_OVERLAP=50`。
- 如果超长内容属于某个标题章节，每个切出的块都会重复带上该标题行，保证 embedding 时保留章节语义。
- `KnowledgeChunk.chunk_id` 使用 `{source}#{chunk_index}`，保证同一输入重复分块时 ID 确定。
- `EmbeddingConfig` 复用 `OPENAI_API_KEY` 和 `OPENAI_BASE_URL`，新增 `EMBEDDING_MODEL`。
- `EmbeddingClient.embed_texts()` 支持批量文本；空列表直接返回空列表，不触发 API 调用。
- OpenAI SDK 抛出的异常统一包装成 `EmbeddingError`，后续工具层可以转换为 `embedding_failed`。

### 验证结果

```powershell
.\.venv\Scripts\python.exe -m pytest tests\test_knowledge_chunker.py tests\test_embedding_client.py -q
```

结果：`11 passed`

追加“超长标题章节每块保留标题行”用例后，最新结果：`12 passed`

全量回归：

```powershell
.\.venv\Scripts\python.exe -m pytest tests -q
```

结果：`110 passed`

### 尚未完成

- [ ] `app/repositories/knowledge_repository.py`
- [ ] `scripts/build_knowledge_index.py`
- [ ] `app/services/agent/tools/search_learning_notes.py`
- [ ] `ToolRegistry` 注册 `search_learning_notes`
- [ ] `personas.py` 接入学习笔记检索提示词
- [ ] README / 架构文档的最终 v0.3 收尾更新

### 当前边界

- 代码块暂时按普通文本处理，符合 v0.3 设计文档 OP1。
- 分块器当前按字符数切分，不做语义分句；检索质量留到 v0.4 评测集校准。
- 本次没有修改 `react_orchestrator.py`、`executor.py`、`tutor_agent_service.py`。
